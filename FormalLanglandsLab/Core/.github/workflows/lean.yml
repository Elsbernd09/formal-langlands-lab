name: Lean Action CI

on:
  push:
  pull_request:

jobs:
  build:
    name: Build Lean project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
